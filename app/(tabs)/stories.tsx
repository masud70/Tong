import ThemedView from "@/components/ThemedView";
import React from "react";
import { Text } from "react-native";

const stories = () => {
	return (
		<ThemedView>
			<Text className="font-bold text-[#ff01c7]">Home Page</Text>
		</ThemedView>
	);
};

export default stories;
